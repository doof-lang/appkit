# AppKit table sample

This sample demonstrates editable text, checkbox, number, and date columns,
typed native sorting, explicit row reloads, and the standard menu bar that
`runApp()` installs when no custom menu tree is supplied.

```sh
doof run appkit/samples/table
```
